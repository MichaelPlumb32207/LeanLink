# Dependencies
node_modules/
.pnp
.pnp.js

# Next.js
.next/
out/
build/
dist/

# Vercel
.vercel/

# Environment & secrets (never commit)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local
!.env.example

# Logs & debug
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Testing & coverage
coverage/
.nyc_output/

# TypeScript
*.tsbuildinfo

# Caches
.cache/
.turbo/
.eslintcache

# OS
.DS_Store
Thumbs.db

# Editors
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.swp
*.swo
*~

# Florida voter extracts — PII; keep local only, never push to GitHub.
# DOS files are CCC_YYYYMMDD.txt / CCC_H_*.txt (any county, not just Calhoun).
CAL_*.txt
CAL_H_*.txt
[A-Z][A-Z][A-Z]_*.txt
[A-Z][A-Z][A-Z]_H_*.txt
*_H_*.txt
samples/*.txt
samples/*.csv
samples/leanlink-*.csv
!samples/.gitkeep

# Sample client-intake files — contain real personal data; keep local only
Import Example.csv
Import Example.xlsx

# Client-experience demos built from real registration rows — local only
docs/demo-client-experience.html
docs/demo-client-experience-data.json
docs/demo-innings-data.json
docs/demo-innings.html
docs/innings-runs/

# Reference bulk downloads (large; local / Neon only)
data/reference/

# Misc local artifacts
*.pem
*.key
fec-data/
scripts/debug-fec.ts
LeanLink - Political lean - on the record.pdf

# ENH-010 re-pass baselines (voter ids + fusion state — never commit)
repass-baseline-*.json

# Statewide campaign / ops (local disk paths, research dumps, PII-adjacent)
docs/ops/

# Owner OSINT self-tests (personal identifiers)
scripts/osint-self-test*.ts

# Statewide campaign CLIs write into gitignored docs/ops/; keep them local
# until paths and membership JSON are reviewed for a later commit.
scripts/run-campaign-wave.ts
scripts/campaign-board.ts
scripts/export-campaign-run.ts
scripts/build-election-margins.ts

# Commercial pitch / offering / one-pagers — local sales copy, not this repo
leanlink-pitch.html
leanlink-one-pager.html
leanlink-offering.html
onrecord-pitch.html
onrecord-one-pager.html
onrecord-offering.html
docs/evidence-accumulator-pitch.html
LeanLink-Plan.html
