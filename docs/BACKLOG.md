# LeanLink — Backlog (defects · enhancements · features)

The work-item ledger: every defect gets a `DEF-` entry **when it's found**, updated when
it's fixed — root cause included, so lessons aren't re-learned. Discrete enhancements and
features get `ENH-`/`FEAT-` entries. Horizons and priorities live in
[`ROADMAP.md`](ROADMAP.md) (this file is the ledger, not the plan); design rationale lives
in [`DECISIONS.md`](DECISIONS.md); every fixed defect should also add a regression test
row in [`USE_CASES.md`](USE_CASES.md) — the "Regression" column links it.

Status: 🔴 open · 🟡 in progress · ✅ fixed/shipped · ⬜ won't fix (say why)

## Defects

| ID | Status | Pri | Title | Root cause → fix | Regression |
|---|---|---|---|---|---|
| DEF-011 | ✅ 2026-07-07 | P2 | Evidence filters slow / "don't react until a 2nd click" | Owner screen-read on the 146k Duval upload. Three stacked causes in `evidence-workspace.tsx`: (1) the name box fired a server round-trip **per keystroke** (no debounce); (2) the voter-list fetch was bundled into `refreshAll` alongside `refreshSummary`, so every 5s box-score poll re-ran the heavy 1000-row list query; (3) no request-ordering guard — a filter click's fetch raced the in-flight poll fetch and whichever **resolved last** won, so a click looked ignored until re-clicked. Fix: 300ms debounce on the name filter; voter-list fetch moved to its own effect keyed on `[uploadId, debouncedName, armFilters]`, decoupled from the summary poll; monotonic `voterReqSeq` + AbortController so only the newest response applies. **Enhancement (owner ask):** the list endpoint now returns a true `total` for the current filter and the panel shows a live "N voters match · showing first 1000" counter that updates as filters toggle. | UC-17 filter row |
| DEF-010 | ✅ 2026-07-06 | P2 | Line-score STATE showed PARTIAL as a by-design terminal status for hit-only arms | Owner: "PARTIAL must never be a terminal status by design." `inningFor` (`lib/box-score.ts`) derived STATE from event coverage (`voters_touched >= eligible_in`); Sunbiz only writes an event on an officer match, so its touched count (46,124) is structurally below eligible (146,129) and it read PARTIAL forever despite `arm_runs.status='completed'`. Fix: drive STATE from the run lifecycle (`summary.runs.recent[].status` — already in the summary, previously ignored): completed → **complete** (renamed from `run`; all finished arms now read COMPLETE, matching the pipeline cards), cancelled/failed → partial, event-coverage heuristic kept only as the run-less fallback. PROCESSED now shows `max(run.processed_count, voters_touched)` so multi-segment (FEC) and hit-only (Sunbiz) arms both read their true coverage. No migration. | UC-18 box-score row (completed hit-only arm reads COMPLETE) |
| DEF-009 | ✅ 2026-07-06 | P2 | Tightened re-pass leaves stale evidence events (inflated "confirmed"; risk of stale leans) | Found by owner screen-read during the ENH-012 Sunbiz re-run: box score showed 78,501 "confirmed" when only 25,663 were real. Root cause: `runFreePassForVoter` writes/updates an arm's event only when it finds a hit; when tightened scoring makes a voter stop matching, it wrote nothing and left the pre-gate event in place (upsert replaces, but nothing *supersedes*). 53,236 stale sunbiz + 5,244 stale layer-2 events lingered (625 carrying an old bridge lean). Fix: `deleteVoterArmEvents` (`lib/evidence/ledger.ts`), source-precise, wired into the sunbiz + layer-2 branches so a no-hit pass supersedes the prior event; existing rows reconciled via `scripts/cleanup-stale-sunbiz.ts`. Seeds ENH-010 (re-pass as a product op). | box-score confirmed count post-cleanup (25,663) in PROGRESS |
| DEF-008 | ✅ 2026-07-06 | P1 | Tier 2 per-voter cost: wildcard entity lookups + per-voter label queries (0.6/s even after DEF-007) | Three costs stacked: layer-2 entity lookup used a leading-wildcard LIKE over 14.2M fl_contributions (up to 3×/voter, seconds each); the officer lookup's `($x='' OR col=$x)` optional-param OR blocked the 017 index; two static snapshot-label queries ran per voter. Fixes: prefix-only entity match + migration 018 text_pattern_ops index; explicit zip-required query shape; labels hoisted into FreePassContext. Measured: 0.6/s → **12/s** (ETA 72 h → ~3.5 h). Third member of the leading-wildcard family — grep for `LIKE '%` when touching reference lookups. | Sunbiz run rate in PROGRESS |
| DEF-007 | ✅ 2026-07-06 | P1 | Sunbiz "index fix" (015) didn't help the real query — leading-wildcard LIKE can't use a btree | The lookup matches `officer_name_norm LIKE '%<last>%'`; migration 015's name index served only the equality shape I benchmarked (53 ms), while the live run crawled at 0.5/s (ETA 72 h). Caught by watching the current-inning strip. Fix: migration 017 `(snapshot_id, officer_zip5)` — the lookup already restricts to the voter's zip, so the index narrows to ~hundreds of rows and the LIKE filters in memory; no semantic change. Lesson: benchmark the exact query shape, not a lookalike. Also added time-based worker heartbeats (45 s) so slow arms don't false-trip the stalled warning. | Sunbiz relaunch rate in PROGRESS |
| DEF-006 | ✅ 2026-07-06 | P1 | Same `\b\(` bug in `lib/committee-lean/infer.ts` — Tier 2 (FL contrib) could never read "(REP)"/"(DEM)"/"(PTY)" committee names | Discovered via the Duval Tier 2 first pass: 465 hit voters, **0 settles**, with "DeSantis, Ron (REP)(GOV)" / "Crist, Charlie (DEM)(GOV)" sitting in the *unresolved* pile (521 distinct committees). Same root cause as DEF-005 in a second module; codebase swept — no further instances. Fix: drop the `\b`; Duval fl-contrib re-passed. Lesson: patterns duplicated across modules should share one source (see donation-lean vs committee-lean). | T13.x via re-pass; unresolved census in PROGRESS |
| DEF-005 | ✅ 2026-07-06 | P1 | Party-code patterns `\b\(rep\)`/`\b\(dem\)` never matched — confirmed donor to "TRISHA 4 COLORADO (DEM)" stayed Undetermined | `\b` before `\(` requires a word char adjacent; both the space and paren are non-word, so the regex was unmatchable since the FL patterns were written. D-030 fixed the *data* (party reaches the name); this fixes the *pattern*. Found by owner on Duval row 1619. Fix: drop the `\b`. Duval re-passed post-fix. | T11.9 (now actually exercised) |
| DEF-004 | ✅ 2026-07-05 | P1 | Confirmed FEC donor shows no recipients; lean "party unclear" despite known committee party | Two-fold (D-030): `lookupFecIndexForVoter` dropped the committee master's party code before lean scoring (regex-only lean); confirmed-no-lean events wrote zero recipient lines. Fix `4215c27`: party folded into committee display name so `\(rep\)`/`\(dem\)` patterns fire on authoritative data; both FEC builders itemize receipts + `payload.receipts`. Found by owner on Duval row 113. | T11.9–T11.10 |
| DEF-003 | ✅ 2026-07-05 | P1 | County-scale upload silently fails — no error trail, nothing in DB | Vercel caps request bodies at ~4.5 MB; the dashboard sends the whole file in one request, and the single-transaction ingest rolls back completely on any failure (Duval, 115 MB, never reached our code). Fix `ccb1cdb`: `scripts/ingest-extract.ts` (chunk-committed, resumable, shared insert helper); SETUP §9. | T2.4–T2.6 |
| DEF-002 | ✅ 2026-07-05 | P2 | Mac sleep pauses county-scale CLI runs mid-flight | Long runs (hours) outlive the display-sleep timer; nothing held a power assertion. Fix `e9b9a15`: `lib/cli/keep-awake.ts` (`caffeinate -i -w <pid>`) wired into all four long CLIs. Limit: lid-close still sleeps — chunked commits + resume are the backstop. | SETUP §9 note |
| DEF-001 | ✅ 2026-07-04 | P2 | Lint gate was dead — `next lint` deprecated, findings accumulated unseen | `next lint` removal in Next 15 left the script a no-op. Fix `344982b`: ESLint CLI + flat config via `FlatCompat` (the `eslint-config-next` modules aren't iterable — don't spread them directly; see root CLAUDE.md). | `npm run lint` in pre-push gate |

## Enhancements

| ID | Status | Pri | Title | Notes |
|---|---|---|---|---|
| ENH-019 | 🟡 | P2 | Guided evidence workbench — clickable innings + TurboTax guide rail | **Planned 2026-07-08; full build spec in [`plans/ENH-019-guided-workbench.md`](plans/ENH-019-guided-workbench.md)** (written for hand-off to a smaller builder model). Reviewed by Claude (Opus) against the codebase — plan accurate; 5-item amendment sheet in `~/.claude/plans/gm-claude-i-had-glowing-floyd.md` (chief one: `buildGuidance` keys off inning `state`, not the old `event_count ≥ npas·0.5` heuristic). **Phase 1 ✅ 2026-07-08:** line-score rows are clickable/keyboard-operable and expand into per-arm detail panels — role/explainer (new pure `lib/evidence/arm-details.ts` registry), funnel sentence, this arm's run history (`RunStrip` + recent runs), and the arm's actions moved in from the steps-1–5 list; the FEC API sweep (`FecSweepPanel`, de-wrapped from `pipeline-step`) now lives under a disclosure in the FEC panel. Evidence-action POST machinery extracted to one shared hook (`components/use-evidence-actions.ts`, carries `confirmLongRerun`); old step buttons kept and re-pointed at the hook. No new routes/migrations; one polling loop; STATE unchanged. Phases (2) guide rail replaces the 5 pipeline cards — pure `lib/guidance.ts` + `scripts/smoke-guidance.ts`; (3) review/deliver stages + polish — still to come. Hard rails: one polling loop (D-029), render-once numbers, STATE stays run-lifecycle (DEF-010), no OSINT run button, keep all inline guards, no migrations/routes. |
| ENH-004 | ✅ 2026-07-05 | P1 | Concurrent FEC index match (`--concurrency N`) | The per-voter cost is Neon RTT from the operator Mac, not Postgres work — N workers overlap the waits. Measured on Duval: 2.6/s → **22–23/s at 8 workers** (~8.5×; 15.5 h → ~1.6 h). Workers commit per ~10 voters; voter-scoped writes never contend. Ctrl-C now marks the run `cancelled` in arm_runs. Next lever if ever needed: run adjacent to Neon (us-east-1) for another ~10–30×. |
| ENH-003 | ✅ 2026-07-06 | P1 | Box score Phase B — `arm_runs` live progress (migration 014) | Migration 014 (applied), `lib/evidence/arm-runs.ts` (start/heartbeat/finish, 10-min stale reap, one active run per upload+arm), `summary.runs` (arm_runs ∪ fec_sweep_jobs at read), current-inning strip (progress/rate/ETA/heartbeat/CLI chip/stalled), uploads-list live dot. **Free-pass hardening (2026-07-06):** `loadFreePassContext` hoisted (household index loads once), `claimFreePassRows` on the shared predicate, per-voter `runFreePassVoterWithContext` (voter-scoped → parallel-safe), CLI rewritten (`--steps fl-contrib\|sunbiz\|all`, `--concurrency`, `--start-after`, SIGINT cancel), dashboard free-pass actions get a **5,000-eligible guard** + chunked commits + arm_runs. Calhoun smoke: 736 voters/82 s at 4 workers. Still inline-only: `match-fec-index` API action (small-list path, low priority). |
| ENH-002 | ✅ 2026-07-06 | P2 | Re-run Alachua FEC match post-fixes | 40,374 re-processed in 29 min at 8 workers: **+29 leans (178 → 207), yield 0.44% → 0.51%**, conversion 82%; all timelines now itemize receipts. |
| ENH-001 | ✅ 2026-07-06 | P2 | Sunbiz index performance | Fixed in two steps: migration 015 (name index — turned out not to serve the real leading-wildcard query, see DEF-007) then migration **017** `(snapshot_id, officer_zip5)`, which matches the lookup's actual zip restriction. County-scale step ⑤ unlocked for real; measured rate on relaunch in PROGRESS. |
| ENH-021 | 🔴 | P3 | T2-a: Grok/OSINT officer corroboration to harvest the un-corroborated Sunbiz bucket | **Measure-first revenue idea (owner, 2026-07-08).** The Sunbiz address hard-gate (ENH-012, `scoreSunbizOfficerMatch`) caps name+zip officer matches without a corroborated street at `ambiguous` (≤0.54) — they can never settle (this is the tail of the old 98,650-collision noise). Hypothesis: **a corporate officer is semi-public** (same D-034 lever that made committee classification work — Grok is strong on public entities, weak on anonymous voters), so a targeted Grok/OSINT pass might *corroborate identity from a real public source* (business filing, listing, news naming them as owner/officer) where generic voter OSINT yielded near-zero. **Hard constraints:** (1) corroboration must be **found, never guessed** — settling/billing on manufactured identity re-opens the 98k false-positive wound in a *billed* product; (2) identity alone doesn't pay — a lean still needs the officer's **company to have partisan FL donations** (the layer-2 bridge), so the addressable set is the *intersection* (un-corroborated officer) ∩ (company has partisan FL contributions); (3) it's a **paid-per-voter Grok arm** — same governance as OSINT (hard `--max-usd` cap, `--dry-run`, owner-gated). **Design:** pre-filter the intersection deterministically (free), Grok only that small set. **Do first:** a control test on a sample (à la `scripts/osint-control-test.ts`) to measure the real corroboration yield + addressable-revenue size *before* building the arm — if committee-like, build "T2-a"; if OSINT-like, skip. |
| ENH-020 | 🔴 | P3 | Reference-data freshness surface + retire the FEC-sweep backend | Open question from D-035 (owner, 2026-07-08). The FEC live-API sweep is gone from the UI; two follow-ups: (a) a **read-only** "reference data" panel showing which `fec_indiv`/`fl_contributions`/`sunbiz` snapshots are loaded and how fresh (`reference_snapshots.completed_at`) — low-risk, directly answers "is my data current?"; (b) fully retire the now-UI-unreachable FEC-sweep **backend** (route `app/api/uploads/[id]/fec-sweep`, worker `app/api/fec-sweep/[id]/worker`, `fec_sweep_jobs` reads in `summary.runs`/`box-score` liveArms) if we're sure no historical sweep run needs to render. **Do NOT** add a trigger-a-reload button — a multi-GB bulk load can't run in Vercel serverless and a partial load poisons matches; reloads stay owner-gated CLI maintenance (`import-fec-indiv.ts`), same posture as OSINT. |
| ENH-005 | 🔴 | P2 | Label top Duval FL committees (researcher workflow) | After DEF-006, the remaining unresolved committees are the genuinely ambiguous ones — top of census: Palm Beach PBA (61 voters), Realtors PAC (36), Empower Parents PAC (19), NRA PVF (12). **Now surfaced in-app** (2026-07-06): `summary.committees` counter + flag line next to the Committee lean labels button (live — labeling shrinks it on the next poll; migration 016 partial index keeps the poll cheap). |

| ENH-006 | ✅ 2026-07-06 | P1 | Funnel baselines + anomaly flags per arm run | `lib/evidence/run-baselines.ts`: median bands (⅓×/3×) over prior completed runs of the same arm (cross-upload, ≥1,000 processed both sides; fec_sweep_jobs join with NULL lean count); computed at read only while runs are active; amber ⚠ lines in the RunStrip. Golden (h) pins the math with the Duval zero-settle shape. |
| ENH-007 | ✅ 2026-07-06 | P1 | Golden-voter canary fixtures | `scripts/smoke-golden-voters.ts` — 26 assertions over synthetic fixtures pinning DEF-005/006 + D-030 regressions, identity gating, conflict fusion, scorer_v stamps, registry-seed parity (DB vs fallback over 17 probes × 2 scopes), anomaly-band math; `--offline` mode; exit-code CI-able. Caught a real subtlety on its first run (single-receipt confidence deflation). |
| ENH-008 | ✅ 2026-07-06 | P1 | Unified data-driven lean-pattern registry + scorer version stamps | Migration 019 `lean_patterns` (35 seeds, per-scope order preserved — see D-031); `lib/lean-patterns/{patterns,registry}.ts` single source + fallback; threaded through the full FEC chain (index CLI/API, sweep worker, retry cron, sync, rescore, disambiguate) and FL chain (free-pass context, refusion, queue, summary counter); `scorer_v` stamped in all four event builders; registry stats line in the committee manager. Editing via SQL (owner call). |
| ENH-008-UI | 🔴 | P3 | Pattern-registry editor UI + refusion-on-pattern-edit | Editor in the committee manager (regex validation UX, priority handling); pattern edits trigger targeted re-pass via `scorer_v` filtering. Deferred from W1 (SQL editing suffices for a single operator). |
| ENH-009 | 🔴 | P2 | Format sentinels at intake seams | Schema fingerprints on client files + reference loads: column counts, field-shape sanity, reject-rate thresholds, layout/row-count deltas vs prior cycle. Upstream protocol changes fail loudly at the door. Resilience #3. |
| ENH-010 | ✅ 2026-07-06 | P2 | Re-pass as a product operation with diff report | "Re-score with current logic" + before/after delta (settles gained/lost, leans gained/lost/flipped, confidence movement, settled-by-arm, why). Two halves: **supersede** (DEF-009 `deleteVoterArmEvents` — a re-pass self-cleans stale events) + **diff** (this): `lib/evidence/repass-diff.ts` (pure `captureRepassSnapshot`/`diffRepassSnapshots`/`formatRepassDiffMarkdown`) + `scripts/repass-diff.ts` CLI (`snapshot` before / `report` after, bracketing any re-pass; JSON baseline, read-only, no migration). Validated on live Duval (self-diff reconciles: 478 settled = 470 fec + 8 fl_contrib, 524 partisan). Golden (m) pins the DEF-009 supersede shape. UI button deferred to ENH-011 (deliverable-delta attachment). Resilience #7. |
| ENH-011 | 🔴 | P2 | Deliverable versioning + delta reports; status-snapshot export | Post-delivery improvements ship as an evidence-backed diff (trust feature, billable refresh); box score exports a client-ready status paragraph. Resilience #8–9. |

| ENH-012 | ✅ 2026-07-06 | P2 | Sunbiz officer identity gate too loose (or demote arm) | Duval measured: 99,360 officer-name hits (68%) / 98,650 "confirmed" / **0 settles** — last-name+zip matching against 20.6M officers is mostly collisions. **Fix (tighten in place, owner call):** street-address corroboration is now a **hard gate** in `scoreSunbizOfficerMatch` (`lib/sunbiz/lookup.ts`) — a match without a corroborated street (`officer_address` vs the voter's `residence.line1`) caps below 0.55, so collisions land at `ambiguous` (never settle) while real officers-at-the-voter's-address still reach `confirmed`. Plus **per-person hit clustering** in the layer-2 bridge (`lib/free-pass/run-voter.ts`): only address-corroborated officers bridge to their corp's donations. No migration (address already stored/selected; matched in-memory over the migration-017 zip window). Goldens (i)/(j). Measured after-numbers → PROGRESS. |
| ENH-018 | ✅ 2026-07-07 | P1 | Committee classification — recover the confirmed donors stuck Undetermined | Measured: **208 of 678 confirmed Duval FEC donors (31%) sit Undetermined** because their committee has no party code / no pattern. Grok is bad at anonymous voters but *great* at classifying public committees — so the fix is Grok-on-committees, not voters. **Unit A (wiring):** FEC Tier-1 scorer consults `committee_lean_labels` (`inferContributionLean` → label wins over pattern; threaded through `scoreFecLookupForVoter`/`inferLeanFromDonations`/`run-index-upload`; loaded at CLI + API). Federal+state share the name-keyed namespace. Golden (n). **Unit B (classifier):** migration **020** (`'agent'` source), source-aware `upsertAgentCommitteeLabel` (agent never overwrites human; human override reclaims `source='researcher'` → **locks** the committee — a bug the rolled-back precedence test caught), `lib/committee-lean/classify.ts` (batched Grok, no web search, **bipartisan→Undetermined**), `scripts/classify-committees.ts` (census FEC+FL → `--dry-run`/propose/`--apply`; billed-account gate: agent proposes, human confirms). **Measured on Duval:** 337 unresolved committees / 365 donors; top-25 classify (\$0.004) → 11 partisan (176 donors: Harris VF→Left(100), NRA→Right(100), Team Kennedy→Ind, Remove Ron→Left…) with every corporate PAC (CSX/Realtors/GuideWell/UPS) correctly left Undetermined. |
| ENH-018-UI | ✅ 2026-07-07 | P2 | Committee manager v2 — review/override agent labels + re-fusion visibility | Owner screen-read found three gaps: (1) modal flashed "NEEDS LABEL (0)/no committees" for ~5s (no loading guard) → gate empty states behind `!loading`; (2) labeled section was read-only and hid provenance → now shows a **source badge** (Grok/Researcher), confidence, and Grok's reasoning, with **edit** (override → reclaims `source='researcher'`, locks the agent) and **delete** (reverts affected voters); (3) committee labels only reach voters after re-fusion, and `classify --apply` left **304 Duval FL voters labeled-but-unfused** with no visibility → new `countPendingRefusion` + a **"Re-fuse now"** action (`refusionAllPendingForUpload`) surfaced in the manager and the box-score "on base" strip (`refuse_committees` opportunity). Bulk re-fuse re-matches FL contribs per voter (~0.5–1s each), so it's guarded at 150 voters inline (>that → CLI, mirrors the free-pass guard); `classify --apply` now re-fuses the FL side too so future runs don't create limbo. Files: `lib/committee-lean/{store,queue,refusion}.ts`, `app/api/committee-lean/route.ts`, `components/committee-lean-manager.tsx`, `lib/box-score.ts`, `lib/evidence/{ledger,types}.ts`, `components/evidence-workspace.tsx`, `scripts/classify-committees.ts`. No migration. |
| ENH-017 | ⬜ 2026-07-07 | P2 | Public follow-graph lean signal (Bluesky first) — **scorer shelved, coverage≈0** | Built `lib/bluesky/client.ts` (public AppView API — searchActors/getFollows/getProfile, no auth) + `scripts/bluesky-coverage-probe.ts` and measured **before** building the scorer. **Result on 30 eligible Duval voters: 33% share a name with *some* Bluesky account, but 0% are confidently linkable** (0 email-handle matches, 0 FL signals). Eyeballing the 9 name-matches proved them other people — a UK Labour MP, a Brown postdoc, an NYC White-House alum. The wall is **identity linkage of a private individual to a social account** — the same wall as the OSINT arm — not graph mechanics. **Decision: don't build the political-account registry + scorer for anonymous NPAs.** Client + probe kept as reusable instruments (re-run per client list — a more public-facing population would score higher). Reinforces: T3 = persona-enrichment, not a lean-settle arm. X unchanged (no clean API + same linkage wall). |
| ENH-016 | 🟡 2026-07-07 | P1 | OSINT (T3) re-scope — stop re-searching donations, aim at public expression | **Finding (control test, `osint-control-test.ts`):** on 10 voters with a *known* signal (5 settled donors + 5 party-registered), grok-full reported a lean on **1/10** and found no trail on 3/5 documented donors — because a big slice of every T3 call was web-searching `fec.gov`/`dos.myflorida.com`/OpenSecrets, the exact data Tiers 1–2 already own deterministically. The OSINT arm predated those indexes and was never re-scoped. **Prune shipped:** `query-builder.ts` `buildPublicExpressionQueries` drops the 5 donation-DB queries (keeps activism, adds endorsement/self-ID/volunteer), `prompts.ts` TIER_A block + modular step-3 now say "public political expression" and "do NOT search FEC/finance sites — resolved upstream." Field name `donations` kept for the payload contract; contents are now public-expression. tsc/lint/build + 32 goldens green. **Remaining (ENH-017):** add the public follow-graph signal (Bluesky) — the legit way to read alignment when people don't author posts. Ad-targeting + sock-puppet FB/IG ruled out (broker channel / covert access / breaks the "public only" promise). |
| ENH-015 | 🟡 2026-07-06 | P2 | Tier-3 OSINT measured cohort — hard-dollar-capped harness | `scripts/run-osint-cohort.ts`: the only arm that spends real money per voter (paid Grok), so the cap is belt-and-suspenders — `--limit` (voter ceiling, default 100), `--max-usd` (hard cap, default $5; stops **before** a voter that could breach it), `--est-usd` (per-voter guard). Cap binds on `max(reported spend, processed × est)` so a $0-reporting API can't run away; runs **sequentially** to keep the cap exact. `--dry-run` previews the cohort + projected cost with **zero** Grok calls. Two cohorts: `rich` (email/history-bearing eligible voters → Tier-3 *upper-bound* yield) / `sample` (deterministic md5 spread → representative). Writes OSINT evidence + fuses (settle/tier-fee only if the upload has a billing account — FL-extract never does); arm_runs lifecycle; bracket with `repass-diff`. **Verified dry-run on Duval:** 100 eligible (rich: 100/100 email+history; sample: 21 email/68 history), cap projection correct. **Execution owner-gated** (real spend; owner-approved ~100-voter Duval cohort, single-digit-dollar cap). |
| ENH-014 | 🟡 2026-07-06 | P2 | FEC cycle backfill — multi-snapshot matching (indiv22/indiv20) | The FEC index lookup matched only the **single latest** `fec_indiv` snapshot, so loading older cycles wouldn't be searched. **Code shipped:** `getActiveFecIndivSnapshotSet` (all completed snapshots, newest-first, combined provenance label) + `lookupFecIndexForVoter(client, ids[], …)` on `snapshot_id = ANY` (existing name indexes cover it — no migration); threaded through `run-index-upload`, `run-fec-index` CLI, and the dashboard `match-fec-index` action. Verified live: the `ids[]` path returns identical hits to the old single-snapshot path (8/13/0 on Duval samples). Each cycle loads under its own `--label` and joins the match set automatically. **Remaining (operator-gated):** the indiv22/indiv20 downloads (~2 GB each) + loads (SETUP §8a), then a `repass-diff`-bracketed re-match to record the Tier-1 lift. |
| ENH-013 | ✅ 2026-07-06 | P2 | Cross-arm identity-scoring upgrades (street corroboration + recency zip penalty) | Companion to ENH-012, same shared helper `lib/reference-data/address-match.ts` (one place, no per-arm fork — DEF-005/006 lesson). fl_contrib (`lib/fl-contrib/identity-match.ts`): street corroboration as a **soft bonus** (lifts weak-band matches that identity gating otherwise withholds) + **recency-aware zip penalty** (a zip match decays from full weight ≤4 yr to 0.5× ≥12 yr — address churn makes a stale zip weaker proof of current identity). FEC unchanged (bulk file has no street field). `SCORER_VERSION` → 3. Goldens (k)/(l). |

## Features

| ID | Status | Pri | Title | Notes |
|---|---|---|---|---|
| FEAT-002 | ✅ 2026-07-05 | — | County-scale extract ingest CLI | `scripts/ingest-extract.ts`; born from DEF-003. |
| FEAT-001 | ✅ 2026-07-05 | — | Box-score dashboard Phase A | Pinned scoreboard + line score + one polling loop (D-029). |
